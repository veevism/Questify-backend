spring:
  datasource:
    url: jdbc:sqlserver://questify-sql.database.windows.net:1433;database=Questify-DB;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
    username: questify-admin@questify-sql
    password: Zomsantech##1597532486
    driver-class-name : com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl : true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        naming:
          physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
          implicit-strategy: org.springframework.boot.orm.jpa.hibernate.SpringImplicitNamingStrategy

#        dialect: org.hibernate.dialect.SQLServer2012Dialect

server:
  servlet:
    context-path: /api/v1

jwt:
  secret: questify_jwt_token_naja_don't_peak_na



